spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:mem:${DATABASE_NAME:rtv_app}

logging:
  level:
    org.springframework.security: trace
    xyz.catuns: debug
    org:
      slf4j: debug
